## Code Comments

Comments explain **why**, not **what**. Skip if the function name and types are self-explanatory.

Comment when:
- Business decisions or domain logic not obvious from code
- Workarounds/hacks with reason
- Non-obvious performance or security constraints

Do not comment obvious operations (`// Get the user`, `// Loop through bookings`).
